# Install torch and torchaudio from the official cu124 index first.
# This file intentionally omits datasets[audio]: manifest-based remote
# training only needs pyarrow and soundfile, while newer datasets extras can
# pull torchcodec and replace the pinned CUDA build of PyTorch.
transformers==4.57.6
accelerate==1.14.0
jiwer==4.0.0
soundfile==0.14.0
PyYAML==6.0.3
numpy>=1.24,<3
pyarrow>=18,<20
scikit-learn>=1.3,<2
